{% extends 'base.html.twig' %}

{% block body %}

    <h2 class="mt-4">Create product</h2>
    <hr>

    <div class="row">
        <div class="col-md-7">
            {{ form_start(form) }}
            {{ form_widget(form) }}
                <button class="btn btn-primary">Create</button>
            {{ form_end(form) }}
        </div>
    </div>


{% endblock %}